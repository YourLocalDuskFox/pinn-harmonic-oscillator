# pinn-harmonic-oscillator
A PyTorch neural network trained to solve the simple harmonic oscillator equation d²x/dt² + ω²x = 0 without any training data, using only the physics itself as the loss signal. Built for INFO 4160 Industrial Internet of Things. Equation → loss function → solution.
